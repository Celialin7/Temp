import pandas as pd
import numpy as np

# ==========================================
# 1. 定义 2025 年澳门假期 (手动列表，最稳健)
# ==========================================
# 包含：元旦, 农历新年, 清明, 耶稣受难, 复活节前日, 劳动, 佛诞, 端午, 
# 国庆, 中秋, 重阳, 追思, 圣母无原罪, 回归, 冬至, 圣诞
# 注意：numpy会自动处理周末，所以如果节日撞上周末，列在这里也没关系，不会重复扣除
macau_2025_holidays = [
    '2025-01-01', # 元旦
    '2025-01-29', '2025-01-30', '2025-01-31', # 农历新年 (初一至初三)
    '2025-04-04', # 清明节
    '2025-04-18', # 耶稣受难日 (银行休)
    '2025-04-19', # 复活节前日 (银行休)
    '2025-05-01', # 劳动节
    '2025-05-05', # 佛诞节
    '2025-05-31', # 端午节
    '2025-10-01', '2025-10-02', # 国庆节及翌日
    '2025-10-07', # 中秋节翌日
    '2025-10-29', # 重阳节
    '2025-11-02', # 追思节
    '2025-12-08', # 圣母无原罪瞻礼
    '2025-12-20', # 澳门特区成立纪念日
    '2025-12-21', # 冬至
    '2025-12-24', # 圣诞节前夕 (澳门银行特色，通常全日或半日休，算全日较安全)
    '2025-12-25', # 圣诞节
]

# 转换为 numpy 需要的格式
holidays_np = np.array(macau_2025_holidays, dtype='datetime64[D]')

# ==========================================
# 2. 计算 Business Days (核心逻辑)
# ==========================================
# 假设 df 是你的数据
# df = pd.DataFrame({'end_date': ['2025-02-10'], 'start_date': ['2025-01-20']}) 

# 确保转换为 datetime 格式
df['col1'] = pd.to_datetime(df['col1'])
df['col2'] = pd.to_datetime(df['col2'])

# 计算工作日天数 (busday_count 能够极快地处理数百万行)
# weekmask='1111100' 表示周一到周五工作
# 结果是 (col1 - col2) 的工作日差
df['bd_diff'] = np.busday_count(
    df['col2'].values.astype('datetime64[D]'), 
    df['col1'].values.astype('datetime64[D]'), 
    weekmask='1111100', 
    holidays=holidays_np
)

# 生成 Exception
df['exception'] = np.where(df['bd_diff'] > 10, 'yes', 'no')

# 打印结果验证
print(df.head())
